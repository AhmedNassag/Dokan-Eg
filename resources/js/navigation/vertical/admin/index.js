import { createNavItems } from '../shared/index'

export default createNavItems('admin', ['dashboard', 'category', 'country', 'city', 'area', 'branch', 'shipping-company', 'language', 'translation', 'user', 'role', 'permission', 'order'])
