import { createNavItems } from '../shared/index'

export default createNavItems('marketer', ['dashboard', 'country', 'city', 'area', 'branch', 'shipping-company', 'language', 'translation', 'order'])
